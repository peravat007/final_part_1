# final_part_1
1.มันมีทั้งดีและไม่ดีครับ waterfall ก็จะมีการทำงานเป็นขั้นเป็นตอน แต่จะใช้เวลานาน ส่วนagile จะง่ายและไม่ซับซ้อนมาก agile จึงสะดวกกว่า และโอเคกว่า
2.git กับ cvs หรือ svn เป็นระบบที่เหมือนกัน การที่เราใช้ git ก็เหมือนเราใช้ cvs ที่ฟรี  
3.
•	git branch feature1
•	git commit -m "first branch"
•	git remote add origin https://github.com/peravat007/feature.git
•	git push -u origin master
4.เพื่อการแก้ปัญหา conflict เราควรใช้ github และควรศึกษาคำสั่งต่างๆ เช่น  commit เพื่อบอกให้เพื่อนรู้ว่าเราแก้อะไรไปบ้าง
5. abcde
6.การเอาโปรแกรมลงเครื่องก็มีข้อดีคือเราสามารถใช้โปรแกรมได้ในครั้งต่อไป แต่มันจะเปลืองความจำ 
7.
	user เข้า Rails router
	ร้องขอหน้า index ไปยัง Controller
	Controller ทำการเลือก Model ที่ตรงกับข้อมูลของผู้ใช้ที่ส่งข้อมูลเข้ามา 
	Model ค้นหาข้อมูลใน database 
	Model ส่งข้อมูลกลับให้ Controller
	Controller ส่งข้อมูล ไป view 
	View ส่ง html กลับ controller 
8.	Controller ส่ง html ให้ เบราเซอร์
8. seagull framework ข้อดี เป็น MVC Framework, มีระบบ template, code ที่เขียนเป็น OOP
9. Heroku เป็น Platform as a Service (Paas) โดยรองรับภาษาโปรแกรมที่หลากหลาย เช่น Ruby, PHP, Node.js, Python, Java, Clojure, Scala
10.เพราะในอนาคต นิสิตบางคนอาจต้องเจอกับปัญหาของการทำงานเป็นทีมที่ไม่มีระบบระเบียบ  จึงได้บรรจุวิชานี้ เพื่อให้การทำงานง่ายขึ้น 
     


